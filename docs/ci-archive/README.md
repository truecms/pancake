# CircleCI Archive

The legacy CircleCI configuration is stored in `circleci-config.yml` for reference. GitHub Actions now provides
all build, test, and release automation. Do not resurrect the CircleCI workflow without updating these docs.
